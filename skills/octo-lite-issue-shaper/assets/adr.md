# <Number>: <Decision Title>

Follow the target repo's `AGENTS.md` format signal. This Markdown template is
the default for unsignaled or `Spec format: markdown` repos. In a
`Spec format: spec-chat` repo, create the corresponding `*.spec.html` ADR with
the same durable sections, anchored blocks, one sentence per prose line, and
pretty-printed semantic-island JSON only where a visual explanation is useful.

## Status

Proposed

## Context

TBD

## Decision

TBD

## Consequences

TBD

## Links

TBD

For spec-chat documents, the HTML is canonical and is reviewed through the
serve → annotate → hand off → drain → edit → reply loop. Do not regenerate an
ADR Markdown counterpart.
